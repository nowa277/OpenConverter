<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Solid dark color so any launcher-mask shape stays dark.
         We avoid <aapt:attr><gradient/></aapt:attr> on the background layer
         because some Pixel launcher renderers ignore the gradient there
         and fall back to pure black. -->
    <path android:fillColor="#121212"
        android:pathData="M0,0 L108,0 L108,108 L0,108 Z"/>
</vector>