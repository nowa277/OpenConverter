package com.openconverter.app.ui.theme

import androidx.compose.ui.graphics.Color

/** Spotify dark palette. Locked from spec §6.1; no light variant in v1. */
val OcBackground       = Color(0xFF121212) // app background
val OcSurface          = Color(0xFF181818) // cards
val OcSurfaceVariant   = Color(0xFF1F1F1F) // pill buttons / chips
val OcPrimary          = Color(0xFF1ED760) // single accent: CTA + active progress + complete
val OcOnPrimary        = Color(0xFF000000)
val OcOnBackground     = Color(0xFFFFFFFF)
val OcOnSurface        = Color(0xFFFFFFFF)
val OcOnSurfaceVariant = Color(0xFFB3B3B3) // secondary text
val OcError            = Color(0xFFF3727F)
val OcOutline          = Color(0xFF7C7C7C)
